# Identity

Canonical public organization identity for `0xda-market`.

## Canonical identity

- **type:** organization
- **id:** `0xda-market`
- **display name:** `0xda-market`
- **GitHub namespace:** `github.com/0xda-market`

The machine-readable source is [`identity.yaml`](identity.yaml). Its type and id must match `manifest.yaml -> mind.subject` exactly.

The identity remains provider-independent even though the current GitHub namespace uses the same string. Equality with a provider login is not evidence or authority for the canonical id.

This concrete Mind consumes Mind Protocol `1.0.0-rc.2` and now publishes a canonical compact emblem. The SVG is the canonical primary mark; the 1024 PNG used by provider surfaces is a deterministic projection of the same controlled geometry. Provider avatars remain consumers and never become identity authority.

## Canonical visual mark

- **asset ref:** `0xda-market-compact-emblem`
- **source:** [`assets/visual/0xda-market/compact-emblem.svg`](../../assets/visual/0xda-market/compact-emblem.svg)
- **primary color:** `#93C482`
- **provider projection:** deterministic 1024×1024 white-canvas PNG generated by `scripts/export_visual_assets.py`

The three green facets form an exchange/cube cell around negative-space channels. The mark keeps the market/cube semantic direction while matching the shared aiaiaiai faceted family.

## Scope

This module owns durable identity facts and canonical visual-asset resolution for the organization. Parent/child ecosystem relationships, provider bindings, products, projects, and runtime configuration require their own explicit contracts and are not inferred here.

<!-- © 2026 aiaiaiai · aiaiaiai.org -->
